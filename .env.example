# Vercel Postgres — get these from Vercel dashboard → Storage → your DB → .env.local tab
DATABASE_URL="postgres://..."
DIRECT_URL="postgres://..."

# Set to "true" locally to enable the Scrape button / scrape API
# Leave unset on Vercel (scraping runs locally against the cloud DB)
SCRAPE_ENABLED="true"
